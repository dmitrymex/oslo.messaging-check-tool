============
Installation
============

At the command line::

    $ pip install oslo.messaging-check-tool

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv oslo.messaging-check-tool
    $ pip install oslo.messaging-check-tool
